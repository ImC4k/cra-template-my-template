import { find } from 'lodash';
import routes from '../routes';

// export const getPagePath = (id) => {
//     const pageRoute = find(routes, {name: 'Page'});
//     return pageRoute.path.replace(':id', id);
// };